def increment(number, by):
    return number + by

# print(increment(2, 1))        *
# result = increment(2, 1)  same*
# print(result)  but longer*
print(increment(2, by=1)) # keyword argument makes code more readable